## pmux models aliases set

Alias a name to an exact model ID

```
pmux models aliases set <channel> <alias> <model-id> [flags]
```

### Options

```
  -h, --help   help for set
```

### Options inherited from parent commands

```
      --config-dir string   override the PMux config root
      --json                emit machine-readable JSON or NDJSON
      --verbose             write safe diagnostic details to stderr
      --yes                 accept the command's defined confirmation
```

### SEE ALSO

* [pmux models aliases](pmux_models_aliases.md)	 - Manage per-channel model aliases

